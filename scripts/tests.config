# tests.config — fuzz 目标列表
#
# 格式：每行一个目标名称（与 src/test/fuzz/ 中 FUZZ_TARGET 宏的名称对应）
# 以 # 开头的行为注释，空行忽略。
#
# 用法示例：
#   ./scripts/fuzz.sh --bitcoin-dir /path/to/bitcoin --time 300
#   ./scripts/fuzz.sh --config /path/to/other.config --skip-build

# ── cluster_linearize 相关目标 ──────────────────
# 主线性化函数（对比生产代码与参考实现）
clusterlin_linearize

# PostLinearize 正确性
clusterlin_postlinearize
clusterlin_postlinearize_tree

# DepGraph 构造与查询
clusterlin_depgraph_sim

# ── txgraph ─────────────────────────────────────
txgraph
